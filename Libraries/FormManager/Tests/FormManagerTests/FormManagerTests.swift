import XCTest
@testable import FormManager

final class FormManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(FormManager().text, "Hello, World!")
//        print( "Background ", (FormManager.shared.theme as! BundleFormPoweredTheming).formConfiguration?.secondAddition)
    }
}
