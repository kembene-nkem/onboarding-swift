import XCTest
@testable import DepositComponentIntegration

final class DepositComponentIntegrationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DepositComponentIntegration().text, "Hello, World!")
    }
}
